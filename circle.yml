machine:
  timezone:
    UTC

  php:
    version: 5.6.14

dependencies:
  pre:
    - curl -sS https://getcomposer.org/installer | php
  override:
    - composer install --prefer-source --no-interaction

test:
  override:
    - ant build